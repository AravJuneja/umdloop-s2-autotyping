# One image point, in pixels, on the convention OpenCV reports corners in:
# the origin sits at the centre of the top-left pixel, u runs right, v runs
# down. Floating point because a refined corner lands between pixels.
float64 u
float64 v
